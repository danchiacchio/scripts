# 🛠 py vSphere Tools (Python)

A small and handy Python script to connect to VMware vCenter and retrieve info about your VMs using `pyVmomi`.

## 📦 Requirements

- Python 3.6+
- `pyvmomi`
- `pyVim`

Install dependencies:
```bash
pip install pyVim pyvmomi
